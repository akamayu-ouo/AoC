#!/usr/bin/env dyalogscript
raw ← { ⊃ ⎕nget ('in/',⍵,'.in') 0 }
lines ← { ⊃ ⎕nget ('in/',⍵,'.in') 1 }

d01a ← { +⍥|.-⌿{⍵[⍋⍵]}¨↓⍉↑⍎¨⍵ } ⍝ lines '01'
d01b ← { {+⌿⍺+.×⍺∘.=⍵}⌿↓⍉↑⍎¨⍵ } ⍝ lines '01'

chk  ← ∘.(⍬≡~)∘(↓1 ¯1∘.×⍳3)-⌿¨⍨∘2
d02a ← { 1⊥,chk⍎¨⍵ }
d02b ← { 1⊥{1∊chk(⊂⍵),↓⍵⌿⍨⍤1∘.≠⍨⍳⍴⍵}∘⍎¨⍵ }
