d01a ← { +⌿-∘2⌊÷∘3↑⍎¨⍵ }
d01b ← { +⌿∊¯1↓¨0⌈(⊢,⍨¯2+⊃⌊⍤÷3⍨)⍣(0≥⊃⍤⊣)∘⍎¨⍵ }


step ← { ⎕io←0
     a b c d ← ⍵[(⍳4)+i←⊢⌿⍵]
     v ← (++(a≠1)××-+)⌿⍵[b c]
     v@d⊢⍵,4+i
}
run ← { ⎕io←0 ⋄ ⊃step⍣{99=⍺[⊢⌿⍺]}(⍺@1 2)0,⍨⍎∊⍵ }
d02a ← 12 2∘run
d02b ← { ⎕io←0 ⋄ 19690720⍳⍨run∘⍵¨,⍳2⍴100 }


path ← { ⎕io←0
    d ← 0 1 0 ¯1 0[0 1∘.+⍨'RDLU'⍳⍵[⍸1,⍵=',']]
    r ← ⍎¨(⍵∊⎕D)⊆⍵
    ↓+⍀r⌿d
}
d03a ← { ⌊⌿|+/↑⊃∩⍥path⌿⍵ }
d03b ← { 2+⌊⌿∊(,⍥⊂+.⍳⊂⍤∩)⍥path⌿⍵ } ⍝ +2 as `path` skips 0 0
